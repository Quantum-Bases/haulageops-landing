import { PlatformJobSchedulingPage } from "@/pages/PlatformJobSchedulingPage";

export const metadata = {
  title: "Haulage Job Scheduling Software | HaulageOps",
  description: "Schedule recurring jobs, manage driver availability and plan forward work without rebuilding the same job from scratch each day.",
};

export default function Page() {
  return <PlatformJobSchedulingPage />;
}
