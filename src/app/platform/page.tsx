import { PlatformPage } from "@/pages/PlatformPage";

export const metadata = {
  title: "The Bulk Haulage Operations Platform | HaulageOps",
  description: "Five portals around one live job record for dispatch, drivers, subcontractors, clients, rates, POD, and invoicing.",
};

export default function Page() {
  return <PlatformPage />;
}
