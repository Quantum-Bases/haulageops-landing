import { PlatformFleetManagementPage } from "@/pages/PlatformFleetManagementPage";

export const metadata = {
  title: "Fleet Management for Haulage Operators | HaulageOps",
  description: "Manage your vehicle records, availability, registrations and document expiry in one place — connected to daily dispatch operations.",
};

export default function Page() {
  return <PlatformFleetManagementPage />;
}
