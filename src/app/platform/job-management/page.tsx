import { PlatformJobManagementPage } from "@/pages/PlatformJobManagementPage";

export const metadata = {
  title: "Haulage Job Management Software | HaulageOps",
  description: "Create, assign, track and complete every haulage job in one system. Origins, destinations, materials, quantities, rates and assignments — all connected.",
};

export default function Page() {
  return <PlatformJobManagementPage />;
}
